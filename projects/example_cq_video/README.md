# Example CQ Video

Copy các file sau vào đúng chỗ rồi chạy validate/render:

```text
input/video/scene_01.mp4 ... scene_05.mp4
input/audio/cq_first.wav
input/audio/cq_tired.wav
input/audio/mosquito.wav  (tùy chọn, chưa tham chiếu trong YAML)
```

Input không bao giờ bị công cụ sửa hoặc ghi đè.

