# Font

Có thể đặt font hỗ trợ tiếng Việt (ví dụ `NotoSans-Bold.ttf`) vào đây và dùng `font_file: ../../assets/fonts/NotoSans-Bold.ttf` từ project. Resolver chỉ chấp nhận file trong `repository/assets/fonts`. Nếu file không có, validate cảnh báo và libass dùng font fallback của Windows.
